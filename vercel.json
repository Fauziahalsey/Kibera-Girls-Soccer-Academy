{
  "version": 2,
  "env": {
    "NODE_VERSION": "22.x"
  },
  "build": {
    "env": {
      "NODE_VERSION": "22.x"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}